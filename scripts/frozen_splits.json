{
  "_comment": "Frozen evaluation splits for pre-registered gates (D0). Every measurement below is deterministic; changing an entry requires a new D0-style freeze note in Decisions.md.",
  "vrsbench_val": {
    "n": 200,
    "ordering": "anvesha.evaluate._vrsbench_val() — sorted(Annotations_val/*.json), first n items with caption+objects",
    "purpose": "B3 grounding ensemble gate (IoU@0.5 vs 0.126 + 0.01 margin) + B10 vrsbench_vqa bench",
    "gate_margin": 0.01
  },
  "vrsbench_val_vqa": {
    "n": 300,
    "ordering": "same sorted order; first n annotations carrying qa_pairs",
    "purpose": "B10 bench_vrsbench_vqa (exact-match + abstain)"
  },
  "levir_fulltest": {
    "n": 1500,
    "threshold": 0.85,
    "protocol": "full-split thresholded protocol; canonical artifacts/phase3_levir_fulltest.json (IoU 0.692 / F1 0.818)",
    "purpose": "B7 change-head calibration val logits + any TTA protocol note"
  },
  "cdvqa_val": {
    "records": 16400,
    "protocol": "promotion-gate val split of scripts/train_cdvqa_head.py (canonical 0.7134)",
    "purpose": "B7 cdvqa head calibration"
  },
  "ben_s1s2_val": {
    "subset": "14K co-registered cross-modal pairs (data/bigearthnet_14k)",
    "seed": 42,
    "purpose": "B7 fusion linear head calibration (fixed seed)"
  },
  "golden_intent": {
    "n": 500,
    "file": "scripts/golden_intent.json",
    "source": "mined RSVQA/CDVQA train questions, hand-checked before freeze",
    "purpose": "B2 router intent-accuracy metric (keyword+centroid+rerank)"
  },
  "counting_gate": {
    "metric": "val digit-acc",
    "current": 0.44,
    "purpose": "M2 stretch probe + honesty banner (never hidden below 0.44 claim)"
  }
}
